import { IClassName } from '@/shared/types/shared'
import { FC } from 'react'
import cls from './RoomsListViewInfo.module.scss'

interface Props extends IClassName {
	
}
const RoomsCardViewInfo: FC<Props> = ({className}) => {
	return (
		
	)
}

export { RoomsCardViewInfo }

